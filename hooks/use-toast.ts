"use client";

// Re-export from the provider so every existing import path keeps working.
export { useToast } from "@/providers/toast-provider";
export type { ToastActions } from "@/providers/toast-provider";